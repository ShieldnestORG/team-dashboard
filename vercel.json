{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @paperclipai/shared build && pnpm --filter @paperclipai/ui build && mv ui/dist dist",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "http://31.220.61.12:3200/api/:path*"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
